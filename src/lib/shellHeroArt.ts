/**
 * Arte hero do shell (trem / infra), servida de public/.
 * SVG institucional versionado — garante fundo visível mesmo sem PNG opcional.
 * Para foto: coloque `public/backgrounds/app-shell-home-trains.png` e troque a exportação abaixo.
 */
export const SHELL_HERO_ART_SRC = '/brand/hero-train-silhouette.svg'

/** Hero da página Login (`public/backgrounds/login-hero.png`). */
export const LOGIN_PAGE_HERO_SRC = '/backgrounds/login-hero.png'
